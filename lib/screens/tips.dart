import 'package:flutter/material.dart';

class TipsContainerScreen extends StatelessWidget {
  TipsContainerScreen();

  @override
  Widget build(BuildContext context) {
    return new Center(
      child: Text('Hello from Tips'),
      /* TODO: Start writing tips here */
    );

  }
}
