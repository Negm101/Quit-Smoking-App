import 'package:flutter/material.dart';

class ScheduleContainerScreen extends StatelessWidget {
  ScheduleContainerScreen();

  @override
  Widget build(BuildContext context) {
    return new Center(
      child: Text('Hello from Schedule'),
      /* TODO: Start writing Schedule here */
    );

  }
}
